/**
 * Utility functions used by the distributed communication system.
 * <p>
 * The classes and methods in this package are effectively independent of the
 * parent package, and each stand on their own. This package therefore basically
 * exists as a place to write small library-style routines that were needed in
 * the creation of the distributed communication system, and which were too
 * small to warrant the inclusion of an external library.
 */
package xyz.acygn.mokapot.util;
